# Copy this to .env and fill in your values
GITHUB_PAT=your_fine_grained_pat_here
GITHUB_OWNER=roff-dev
GITHUB_REPO=money-machine
# Absolute path to repo root — only needed if running app.py from a different directory
REPO_ROOT=
# Port for the Flask server (default: 5000)
PORT=5000
